import React, { Component } from 'react';

class Micomponent extends Component{

    render() {
        return(
            <h1> Hola, soy el componente llamado: Micomponent </h1>
        );
    }
}

export default Micomponent;